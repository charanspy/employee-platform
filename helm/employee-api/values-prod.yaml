env:
  ENV: production
replicaCount: 5

image:
  repository: employee-api
  tag: v1
  pullPolicy: IfNotPresent

ingress:
  enabled: true
  ingressClassName: nginx
  hosts:
    - host: employee-prod.local 
      paths:
        - path: /
          pathType: Prefix